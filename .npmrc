legacy-peer-deps=true
# Make the package.json engines floor (Node >=20.9) a hard failure rather than a
# warning. The native deps are N-API, so an install on Node 18 otherwise appears
# to work and only diverges later — @hono/node-server (via the MCP SDK) is the
# dependency that actually requires >=20.
engine-strict=true
