# Keep the build context to source only — deps and build output are produced
# inside the image (native modules must match the image's platform/ABI).
node_modules
.next
.git
.gitignore
.env*
*.md
docs
# the calandria.dev site — built and deployed by .github/workflows/website.yml,
# never served by the app
website
# the desktop shell wraps a running server from the outside; it is never in one
desktop
Dockerfile
docker-compose.yml
.dockerignore
.claude
